{
  "name": "@slack-wrench/bolt-interactions",
  "version": "1.0.0",
  "description": "Easily create testable interaction patterns in bolt.",
  "main": "src/index.ts",
  "scripts": {
    "start": "nodemon scripts/ngrok.ts",
    "test": "yarn lint && NODE_ENV=test yarn jest",
    "lint": "eslint . --ext .js,.ts --max-warnings 0",
    "lint:fix": "yarn lint --fix",
    "type": "tsc --noEmit"
  },
  "license": "ISC",
  "dependencies": {
    "@slack/bolt": "^1.4.1",
    "@slack/types": "^1.3.0",
    "@types/koa-compose": "^3.2.5",
    "@types/shortid": "^0.0.29",
    "koa-compose": "^4.1.0",
    "shortid": "^2.2.15"
  }
}
